'use client';

import Layout from '@/components/Layout';

const AgendaLoader = () => {
  return (
    <Layout>
      <div className="h-[808px] w-full rounded-[4px] overflow-auto bg-white no-scrollbar animate-pulse"></div>
    </Layout>
  );
};

export default AgendaLoader;
